# AWS profile from ~/.aws/config (must have Bedrock access)
AWS_PROFILE=vscode-user
AWS_REGION=us-east-1

# Optional: override default models
# EXTRACTION_MODEL=amazon.nova-lite-v1:0
# ANSWER_MODEL=amazon.nova-pro-v1:0
